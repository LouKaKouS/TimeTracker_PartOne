<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TimeTracker" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.TimeTracker" parent="Base.Theme.TimeTracker" />

    <style name="TitleApp">
        <item name="android:fontFamily">@font/fortuner_heavy_personal_use</item>
        <item name="android:textSize">35dp</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="date_indication">
        <item name="fontFamily">@font/brlnsr</item>
        <item name="android:textSize">25dp</item>
    </style>

    <style name="default_text">
        <item name="android:fontFamily">@font/brlnsr</item>
        <item name="android:textSize">20dp</item>
    </style>
</resources>